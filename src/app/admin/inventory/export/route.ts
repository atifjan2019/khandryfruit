import { db } from "@/lib/db/client"; import { requireAdmin } from "@/server/policies/authorization";
export async function GET() { await requireAdmin("inventory"); const rows = await db.inventory.findMany({ include: { variant: { include: { product: { include: { translations: { where: { locale: "en" } } } } } } } }); const header = "product,sku,weight_grams,on_hand,reserved,available,low_stock_threshold"; const body = rows.map((item) => [item.variant.product.translations[0]?.name ?? item.variant.product.internalName, item.variant.sku, item.variant.weightGrams, item.onHand, item.reserved, item.onHand - item.reserved, item.lowStockThreshold].map((value) => `"${String(value).replaceAll('"', '""')}"`).join(",")); return new Response([header, ...body].join("\r\n"), { headers: { "content-type": "text/csv", "content-disposition": "attachment; filename=khan-inventory.csv" } }); }
